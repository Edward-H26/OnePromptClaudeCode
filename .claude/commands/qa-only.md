---
description: Browser-based QA in report-only mode (no code changes)
argument-hint: "Target URL or instructions"
---

Invoke the `/qa-only` skill. Read the SKILL at `.claude/skills/qa-only/SKILL.md` and follow it exactly.

Run browser-based QA in report-only mode. Do not make code changes from this command.

Target or instructions: $ARGUMENTS
