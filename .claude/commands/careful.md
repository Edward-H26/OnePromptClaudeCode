---
description: Activate safety guardrails for destructive commands
---

Invoke the `/careful` skill. Read the SKILL at `~/.claude/skills/careful/SKILL.md` and follow it exactly.

Activate safety guardrails for destructive commands (rm -rf, DROP TABLE, force-push, git reset --hard).

$ARGUMENTS
