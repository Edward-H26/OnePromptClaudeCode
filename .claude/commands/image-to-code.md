---
description: Generate design images, then implement matching website
argument-hint: "Website or section to build"
---

Invoke the `image-to-code` skill. Read the SKILL at `.claude/skills/image-to-code/SKILL.md` and follow it.

Generate design images first, analyze them deeply, then implement a website that matches them closely, with clean spacious heroes.

Target: $ARGUMENTS
