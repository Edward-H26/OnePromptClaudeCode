---
description: Bulk rename or pattern replacement across the codebase
argument-hint: "What to rename/replace and the target scope"
---

Invoke the `code-refactor` skill. Read the SKILL at `.claude/skills/code-refactor/SKILL.md` and follow it.

Use grep + Edit to perform a bulk refactor with verification at each step.

Target: $ARGUMENTS
