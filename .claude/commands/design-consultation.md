---
description: Generate a design system or visual direction before implementation
argument-hint: "Brief or scope"
---

Invoke the `/design-consultation` skill. Read the SKILL at `~/.claude/skills/design-consultation/SKILL.md` and follow it exactly.

Generate a design system or higher-level visual direction before implementation.

Brief or scope: $ARGUMENTS
