---
description: Test a local webapp end-to-end with Playwright (Python)
argument-hint: "Local app URL or test scenario"
---

Invoke the `webapp-testing` skill. Read the SKILL at `.claude/skills/webapp-testing/SKILL.md` and follow it.

Use the Python-driven Playwright toolkit for local app lifecycle, browser flows, and screenshots.

Target: $ARGUMENTS
