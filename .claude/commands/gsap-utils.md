---
description: gsap.utils helpers: clamp, mapRange, random, snap, interpolate
---

Invoke the `gsap-utils` skill. Read the SKILL at `.claude/skills/gsap-utils/SKILL.md` and follow it.

gsap.utils helpers: clamp, mapRange, random, snap, interpolate
