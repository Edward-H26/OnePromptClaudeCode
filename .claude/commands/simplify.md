---
description: Review changed code for unnecessary complexity
argument-hint: "Scope or focus"
---

Use the installed `code-simplifier` agent to review the current scope for unnecessary complexity while preserving exact behavior.

Focus on the changed files or the explicit scope below. Keep the review bounded, and do not widen the change set without a concrete reason.

Scope or focus: $ARGUMENTS
