---
description: Lazy senior-dev mode: enforce the minimal solution that actually works
argument-hint: "[lite|full|ultra]"
---

Invoke the `ponytail` skill. Read the SKILL at `.claude/skills/ponytail/SKILL.md` and follow it.

Enforce the YAGNI ladder (reuse > stdlib > native > one line). Set intensity, or 'off' to stop.

Target: $ARGUMENTS
