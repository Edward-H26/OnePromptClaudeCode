---
description: Browser-based QA testing with bug fixes and regression tests
argument-hint: "Target URL or instructions"
---

Invoke the `/qa` skill. Read the SKILL at `~/.claude/skills/qa/SKILL.md` and follow it exactly.

This is browser-based QA testing. Test the application, find bugs, fix them, and auto-generate regression tests.

Target or instructions: $ARGUMENTS
